<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= producto.nombre %> - Mi E-commerce</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="bg-light">

    <nav class="navbar navbar-dark bg-dark shadow-sm">
        <div class="container">
        <a class="navbar-brand" href="/">🚀 TechStore</a>
        <div class="navbar-nav">
            <a class="nav-link" href="/categoria/computadores">Computadores</a>
            <a class="nav-link" href="/categoria/perifericos">Periféricos</a>
        </div>
    </div>
    </nav>

    <div class="container mt-5">
        <div class="row">
            <div class="col-md-2">
                <a href="/" class="btn btn-outline-secondary mb-4">
                    <i class="bi bi-arrow-left"></i> Volver al catálogo
                </a>
            </div>
        </div>
        
        <div class="row">
            <div class="col-md-6">
                <img src="<%= producto.imagen %>" class="img-fluid rounded shadow" alt="<%= producto.nombre %>">
            </div>
            <div class="col-md-6">
                <div class="card h-100 shadow-sm">
                    <div class="card-body">
                        <h1 class="card-title mb-3"><%= producto.nombre %></h1>
                        <p class="text-muted mb-2">
                            <strong>Categoría:</strong> 
                            <span class="badge bg-secondary"><%= producto.categoria.charAt(0).toUpperCase() + producto.categoria.slice(1) %></span>
                        </p>
                        <h3 class="text-success fw-bold mb-4">$<%= producto.precio %></h3>
                        
                        <div class="mb-4">
                            <h5>Detalles del Producto:</h5>
                            <% if (producto.detalles && producto.detalles.trim() !== '') { %>
                                <p class="card-text"><%= producto.detalles %></p>
                            <% } else { %>
                                <p class="text-muted fst-italic">Los detalles del producto se agregarán próximamente.</p>
                            <% } %>
                        </div>
                        
                        <div class="d-grid gap-2">
                            <button class="btn btn-success btn-lg">Comprar Ahora</button>
                            <button class="btn btn-outline-primary">Agregar al Carrito</button>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>